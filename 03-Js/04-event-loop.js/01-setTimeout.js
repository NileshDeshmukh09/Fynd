/**
 * setTimeout :- requests runtime to run the function(f)
 *  after 5 seconds 
 *   
 *  setTimeout : ( Asynchronous / Non Blocking )
 */
setTimeout(() => {
    console.log("function is called ..!");
}, 3000);

console.log("End Line");