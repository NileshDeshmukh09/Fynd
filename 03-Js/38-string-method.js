/**
 * reverse()
 */
const primes =[2,3,5,7,11];
primes.reverse();
console.log( primes);

/**
 * concat() -> join 2 or more array 
 */
const composites =[ 4,5,6,7,8];
const primesAndComposites = primes.concat(composites); // does not mute either array
console.log( primesAndComposites );

/**
 * shift() -> remove from the start of the array
 * unshift()-> add to the start of arrray
 * 
 */


/* 
 * splice() ->  add / remove  items at any positions in the array
 */

/**
 * slice() -> get the portion of the array
 */

/**
 * join() -> concatenate string 
 */
